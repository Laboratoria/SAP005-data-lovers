// estas funciones son de ejemplo

export const applySearchType = ( pokemon, filterType) => pokemon. filter ((search)=> (search.type.includes (filterType)));
export  const  resultCalcType  =  ( pokemon,  filterType )  =>
  ( ( filterType ) / ( pokemon ) * 100 ) ;

export  const  applySearchName  =  ( pokemon ,  filterName )  =>  pokemon . filtro ( ( search )  =>
  ( search . name . toLowerCase ( ) . indexOf (filterName . toLowerCase ( ) )  >  - 1 ) ) ;

  export  const  resultCalcType  =  ( pokemon,  filterType )  =>
  ( ( filterType ) / ( pokemon ) * 100 ) ;

  return 'example';
};

/*export const anotherExample = () => {
  return 'OMG';
};*/
